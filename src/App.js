import Router from "./router/Router";

function App() {
  // const selector = useSelector((state) => state);

  return (
    <main className="App">
      <Router />
    </main>
  );
}

export default App;
